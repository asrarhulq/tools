# ---------------------------------------------------------------------------
# Public site URL — canonical URLs, sitemap, robots, OG metadata..
# No trailing slash. In production: https://tools.asrarul.com
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Human-readable site/brand name (metadata + JSON-LD).
NEXT_PUBLIC_SITE_NAME="asrarul.tools"
